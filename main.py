from capture import capture, pull_capture, check_capture
from open_ld import open_ld_player
from play import click_play_now
from run_app import run_app

if __name__ == "__main__":
    # Open LDPlayer and running app
    #open_ld_player()
    #run_app()
    #click_play_now()
    capture()
    pull_capture()
    check_capture()
